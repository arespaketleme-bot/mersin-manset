{
  "name": "mersin-manset",
  "version": "1.0.0",
  "description": "Mersin Manşet web site - Cloudflare Workers deployment",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.5.0"
  }
}
